[gd_resource type="Resource" script_class="WaysideData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/resources/wayside_data.gd" id="1_data"]

[resource]
script = ExtResource("1_data")
id = "overturned_cart"
title = "An Overturned Cart"
text = "A trader's cart lies on its side at the edge of the trees, one wheel still turning. The load is half in the grass. Nobody is with it."
choice_ids = PackedStringArray("cart_right", "cart_strip")
